# Frontend (Vite)
# Use publishable key only (do NOT use secret/service_role in the browser)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY
# Set to app if you keep the custom schema
VITE_SUPABASE_SCHEMA=app

# Optional: feature flags / telemetry
VITE_ENABLE_MOCK_DATA=true

# Supabase CLI / project metadata (never commit real values)
SUPABASE_PROJECT_REF=tiqoyulupyueblevefea
SUPABASE_DB_PASSWORD=YOUR_ROTATED_DB_PASSWORD
