# Database (Supabase)
# Pooler connection for runtime (session or transaction pooler)
DATABASE_URL="postgresql://postgres:${SUPABASE_DB_PASSWORD}@YOUR_POOLER_HOST:6543/postgres?pgbouncer=true"

# Direct connection for migrations (shared pooler, port 5432)
DIRECT_URL="postgresql://postgres:${SUPABASE_DB_PASSWORD}@db.tiqoyulupyueblevefea.supabase.co:5432/postgres"

# Optional: schema if using app instead of public
DB_SCHEMA=app

# Supabase project metadata
SUPABASE_PROJECT_REF=tiqoyulupyueblevefea
SUPABASE_DB_PASSWORD=YOUR_ROTATED_DB_PASSWORD

# NestJS app
PORT=3001
NODE_ENV=development
